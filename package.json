{
  "name": "server",
  "version": "1.0.0",
  "description": "REST API Server For PlayInSync.",
  "main": "./build/index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node ./build/index.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/node": "^18.11.9"
  }
}
