{
  "name": "server",
  "version": "1.0.0",
  "description": "REST API Server For PlayInSync.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/node": "^18.11.9"
  }
}
